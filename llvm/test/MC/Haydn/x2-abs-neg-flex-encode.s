# RUN: llvm-mc -triple=haydn-unknown-elf -show-encoding %s \
# RUN:   | FileCheck %s --check-prefix=ENC
# RUN: llvm-mc -triple=haydn-unknown-elf -filetype=obj -o %t.o %s
# RUN: llvm-objdump -d --no-show-raw-insn --triple=haydn-unknown-elf %t.o \
# RUN:   | FileCheck %s --check-prefix=DIS
# REQUIRES: haydn-registered-target

# Role: object — flex S1/S2 unary DR64 abs/neg forms encode and disassemble.
# Pins live E96 assembler bytes + objdump identity only. No golden invent.

# ENC: x2abs32s{{.*}}encoding: [0x07,0x24,0x09,0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00]
# ENC: x2neg32s{{.*}}encoding: [0x07,0xa4,0x09,0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00]
# ENC: x2abs32{{.*}}encoding: [0x07,0x04,0x09,0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00]
# ENC: x2neg32{{.*}}encoding: [0x07,0x84,0x09,0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00]
# ENC: x2neg32_l{{.*}}encoding: [0x07,0xc4,0x09,0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00]
# ENC: x2neg32s_l{{.*}}encoding: [0x07,0xe4,0x09,0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00]

# DIS-LABEL: <.text>:
# DIS: x2abs32s
# DIS: x2neg32s
# DIS: x2abs32
# DIS: x2neg32
# DIS: x2neg32_l
# DIS: x2neg32s_l
# DIS-NOT: <unknown>

{ x2abs32s d0, d1 }
{ x2neg32s d0, d1 }
{ x2abs32 d0, d1 }
{ x2neg32 d0, d1 }
{ x2neg32_l d0, d1 }
{ x2neg32s_l d0, d1 }
