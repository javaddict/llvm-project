; NOTE: Assertions have been autogenerated by utils/update_llc_test_checks.py UTC_ARGS: --version 6
; RUN: llc -mtriple=haydn-unknown-elf -mattr=-hwloop -global-isel-abort=1 -O2 \
; RUN:   < %s | FileCheck %s --check-prefix=ASM
;
; REQUIRES: haydn-registered-target
;
; This test was XFAIL'd from until reverted the
; GPR-port-FuncUnits change to HaydnSchedule.td that triggered a SIGSEGV in
; the post-RA VLIW scheduler (ConvergingVLIWScheduler::SchedulingCost
; use-after-free on a ready-queue SUnit). The XFAIL is removed now that the
; single-stage slot-only itinerary model is restored; the scheduler runs
; cleanly at -O1+. See decision and lesson.
;
; IIR biquad filter kernel codegen test.
;
; This test exercises the full GISel pipeline on representative IIR filter
; kernels and verifies the assembly output contains expected instruction
; mnemonics (mul32, mac32, sub32, add32, ld32, st32, jalr).
;
; NOTE: The VLIW packetizer is currently disabled (see HaydnTargetMachine.cpp
; line 260). When re-enabled, add back -print-after=haydn-vliw-packetizer MIR
; checks for BUNDLE instructions.
;
; The IIR biquad Direct Form 1 kernel computes:
; yn = b0*xn + b1*x1 + b2*x2 - a1*y1 - a2*y2
;
; This is the most common DSP filter operation and produces a mix of:
; Multiply-accumulate chains (mull + add32; no scalar GPR MAC opcode)
; Subtractions for the feedback path (sub32)
; Loads for coefficient and state access (ld32)
; Stores for state updates (st32)

; Simple IIR biquad inner product: pure register-based ALU.
; Exercises mull + add32.
define i32 @biquad_simple(i32 %b0, i32 %xn, i32 %b1, i32 %x_nm1,
; ASM-LABEL: biquad_simple:
; ASM:       // #<spill-kpi> @biquad_simple spills=0 spill-bytes=0 reloads=0 reload-bytes=0
; ASM-NEXT:  // %bb.0: // %entry
; ASM-NEXT:    { xor32 r0, r0, r0; nop; nop }
; ASM-NEXT:    { subi32 sp, sp, 8; nop }
; ASM-NEXT:    { mull r1, r1, r2; mull r2, r3, r4; nop }
; ASM-NEXT:    { add32 r1, r1, r2; mull r2, r5, r6; nop }
; ASM-NEXT:    { sub32 r1, r1, r2; nop; nop }
; ASM-NEXT:    { xor32 r0, r0, r0; nop; nop }
; ASM-NEXT:    { addi32 sp, sp, 8; nop }
; ASM-NEXT:    { nop; jalr r0, lr, 0; nop }
                          i32 %a1, i32 %y_nm1) nounwind {
entry:
  %prod0 = mul i32 %b0, %xn
  %sum   = mul i32 %b1, %x_nm1
  %acc   = add i32 %prod0, %sum
  %fb    = mul i32 %a1, %y_nm1
  %result = sub i32 %acc, %fb
  ret i32 %result
}

; IIR biquad with memory load/store for state array.
define void @biquad_with_state(i32 %xn, ptr nocapture %state) nounwind {
; ASM-LABEL: biquad_with_state:
; ASM:       // #<spill-kpi> @biquad_with_state spills=0 spill-bytes=0 reloads=0 reload-bytes=0
; ASM-NEXT:  // %bb.0: // %entry
; ASM-NEXT:    { xor32 r0, r0, r0; nop; nop }
; ASM-NEXT:    { subi32 sp, sp, 8; nop }
; ASM-NEXT:    { s_lw_with_imm r3, r2, 0; nop; s_lw_with_imm r4, r2, 1 }
; ASM-NEXT:    { nop; nop; nop }
; ASM-NEXT:    { s_lw_with_imm r5, r2, 2; mull r6, r1, r4; nop }
; ASM-NEXT:    { mull r3, r1, r3; nop; s_sw_with_imm r1, r2, 0 }
; ASM-NEXT:    { add32 r3, r3, r6; nop; s_sw_with_imm r4, r2, 1 }
; ASM-NEXT:    { sub32 r3, r3, r5; nop; nop }
; ASM-NEXT:    { nop; nop; s_sw_with_imm r3, r2, 2 }
; ASM-NEXT:    { xor32 r0, r0, r0; nop; nop }
; ASM-NEXT:    { addi32 sp, sp, 8; nop }
; ASM-NEXT:    { nop; jalr r0, lr, 0; nop }
entry:
  %x1_ptr = getelementptr i32, ptr %state, i32 0
  %x2_ptr = getelementptr i32, ptr %state, i32 1
  %y1_ptr = getelementptr i32, ptr %state, i32 2
  %x1 = load i32, ptr %x1_ptr, align 4
  %x2 = load i32, ptr %x2_ptr, align 4
  %y1 = load i32, ptr %y1_ptr, align 4
  %p0 = mul i32 %xn, %x1
  %p1 = mul i32 %xn, %x2
  %s0 = add i32 %p0, %p1
  %yn = sub i32 %s0, %y1
  store i32 %xn, ptr %x1_ptr, align 4
  store i32 %x2, ptr %x2_ptr, align 4
  store i32 %yn, ptr %y1_ptr, align 4
  ret void
}

; Block processing: apply biquad to N samples in a loop.
define void @biquad_process_block(ptr nocapture %input, ptr nocapture %output,
; ASM-LABEL: biquad_process_block:
; ASM:       // #<spill-kpi> @biquad_process_block spills=7 spill-bytes=28 reloads=7 reload-bytes=28
; ASM-NEXT:  // %bb.0: // %entry
; ASM-NEXT:    { xor32 r0, r0, r0; nop; nop }
; ASM-NEXT:    { subi32 sp, sp, 40; nop }
; ASM-NEXT:    { addi32 r12, sp, 20; nop }
; ASM-NEXT:    { nop; nop; s_sw_with_imm fp, r12, 0 }
; ASM-NEXT:    { nop; nop; s_sw_with_imm r11, r12, 1 }
; ASM-NEXT:    { nop; nop; s_sw_with_imm r10, r12, 2 }
; ASM-NEXT:    { nop; nop; s_sw_with_imm r9, r12, 3 }
; ASM-NEXT:    { nop; nop; s_sw_with_imm r8, r12, 4 }
; ASM-NEXT:    { addi32 r1, sp, 40; move32 r12, r1 }
; ASM-NEXT:    { addi32 r8, sp, 48; s_sw_with_imm r4, sp, 3 } // 4-byte Folded Spill
; ASM-NEXT:    // 4-byte Spill
; ASM-NEXT:    { s_lw_with_imm r4, r1, 0; nop; s_lw_with_imm r8, r8, 0 }
; ASM-NEXT:    { addi32 r9, r0, 0; s_sw_with_imm r3, sp, 4 } // 4-byte Folded Spill
; ASM-NEXT:    // 4-byte Spill
; ASM-NEXT:  .LBB2_1: // %loop
; ASM-NEXT:    // =>This Inner Loop Header: Depth=1
; ASM-NEXT:    { s_lw_with_imm r1, sp, 3; addi32 r9, r9, 1 } // 4-byte Folded Reload
; ASM-NEXT:    // 4-byte Reload
; ASM-NEXT:    { s_lw_with_imm r10, r12, 0; nop; s_lw_with_imm r11, r8, 0 }
; ASM-NEXT:    { nop; nop; nop }
; ASM-NEXT:    { s_lw_with_imm fp, r8, 1; mull r3, r1, r10; nop }
; ASM-NEXT:    { mull r1, r5, r11; addi32 r12, r12, 4 }
; ASM-NEXT:    { add32 r1, r3, r1; mull r3, r6, fp; nop }
; ASM-NEXT:    { s_lw_with_imm fp, r8, 2; add32 r1, r1, r3; nop }
; ASM-NEXT:    { nop; nop; nop }
; ASM-NEXT:    { mull r3, r7, fp; s_lw_with_imm fp, r8, 3; nop }
; ASM-NEXT:    { nop; nop; nop }
; ASM-NEXT:    { sub32 r1, r1, r3; mull r3, r4, fp; nop }
; ASM-NEXT:    { sub32 r1, r1, r3; nop; nop }
; ASM-NEXT:    { addi32 r2, r2, 4; s_sw_with_imm r1, r2, 0 }
; ASM-NEXT:    { nop; nop; s_sw_with_imm r1, r8, 2 }
; ASM-NEXT:    { s_lw_with_imm r1, sp, 4; nop; nop } // 4-byte Folded Reload
; ASM-NEXT:    // 4-byte Reload
; ASM-NEXT:    { nop; nop; s_sw_with_imm r10, r8, 0 }
; ASM-NEXT:    { slt32 r1, r9, r1; nop; s_sw_with_imm r11, r8, 1 }
; ASM-NEXT:    { nop; nop; s_sw_with_imm fp, r8, 3 }
; ASM-NEXT:    { bnez r1, .LBB2_1; nop; nop }
; ASM-NEXT:  // %bb.2: // %exit
; ASM-NEXT:    { xor32 r0, r0, r0; nop; nop }
; ASM-NEXT:    { s_lw_with_imm fp, sp, 5; nop; nop }
; ASM-NEXT:    { s_lw_with_imm r11, sp, 6; nop; nop }
; ASM-NEXT:    { s_lw_with_imm r10, sp, 7; nop; nop }
; ASM-NEXT:    { s_lw_with_imm r9, sp, 8; nop; nop }
; ASM-NEXT:    { s_lw_with_imm r8, sp, 9; nop; nop }
; ASM-NEXT:    { addi32 sp, sp, 40; nop }
; ASM-NEXT:    { nop; jalr r0, lr, 0; nop }
                                  i32 %N, i32 %b0, i32 %b1, i32 %b2,
                                  i32 %a1, i32 %a2, ptr nocapture %state) nounwind {
entry:
  br label %loop

loop:
  %i = phi i32 [ 0, %entry ], [ %next, %loop ]
  %xn_ptr = getelementptr i32, ptr %input, i32 %i
  %yn_ptr = getelementptr i32, ptr %output, i32 %i
  %xn = load i32, ptr %xn_ptr, align 4
  ; Inline 32-bit biquad (no call)
  %x1_ptr = getelementptr i32, ptr %state, i32 0
  %x2_ptr = getelementptr i32, ptr %state, i32 1
  %y1_ptr = getelementptr i32, ptr %state, i32 2
  %y2_ptr = getelementptr i32, ptr %state, i32 3
  %x1 = load i32, ptr %x1_ptr, align 4
  %x2 = load i32, ptr %x2_ptr, align 4
  %y1 = load i32, ptr %y1_ptr, align 4
  %y2 = load i32, ptr %y2_ptr, align 4
  %p0 = mul i32 %b0, %xn
  %p1 = mul i32 %b1, %x1
  %s0 = add i32 %p0, %p1
  %p2 = mul i32 %b2, %x2
  %s1 = add i32 %s0, %p2
  %p3 = mul i32 %a1, %y1
  %s2 = sub i32 %s1, %p3
  %p4 = mul i32 %a2, %y2
  %yn = sub i32 %s2, %p4
  store i32 %yn, ptr %yn_ptr, align 4
  store i32 %xn, ptr %x1_ptr, align 4
  store i32 %x1, ptr %x2_ptr, align 4
  store i32 %yn, ptr %y1_ptr, align 4
  store i32 %y2, ptr %y2_ptr, align 4
  %next = add i32 %i, 1
  %cmp = icmp slt i32 %next, %N
  br i1 %cmp, label %loop, label %exit

exit:
  ret void
}
