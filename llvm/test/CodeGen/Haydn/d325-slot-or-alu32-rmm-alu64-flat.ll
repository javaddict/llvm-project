; NOTE: Assertions have been autogenerated by utils/update_llc_test_checks.py UTC_ARGS: --version 6
; RUN: llc -mtriple=haydn-unknown-elf -global-isel-abort=1 \
; RUN:     -filetype=obj < %s -o %t.o && \
; RUN:   llvm-objdump -d --triple=haydn-unknown-elf %t.o | FileCheck %s
; REQUIRES: haydn-registered-target

; Role: object — encode DR64 compare/add families through the product Format E encoder.

; REGRESSION: s0 ALU32 RI/MOVT/MULL + s1 ALU64-flat families must reach object
; code via the single product Format E encoder (retired dual slot-OR path).
;
; Historical bug: Mode-0 slot-OR finalizer left these families generic and
; forced hand-rolled packInstructionIntoSlot. Product path is single encode.
;
; Load-bearing assertions are the objdump mnemonics for the exercised kernels
; (seq64/slt64/sle64/add64). Byte-equality between two encoder flags is no
; longer required because the dual path is gone; silent empty .text cannot
; pass.


define i64 @seq64_test(i64 %a) nounwind {
  %r = call i64 @llvm.haydn.seq64(i64 %a, i64 %a)
  ret i64 %r
}
declare i64 @llvm.haydn.seq64(i64, i64)

define i64 @slt64_test(i64 %a) nounwind {
  %r = call i64 @llvm.haydn.slt64(i64 %a, i64 %a)
  ret i64 %r
}
declare i64 @llvm.haydn.slt64(i64, i64)

define i64 @sle64_test(i64 %a) nounwind {
  %r = call i64 @llvm.haydn.sle64(i64 %a, i64 %a)
  ret i64 %r
}
declare i64 @llvm.haydn.sle64(i64, i64)

define i64 @add64s_test(i64 %a, i64 %b) nounwind {
  %r = add i64 %a, %b
  ret i64 %r
}
;; NOTE: These prefixes are unused and the list is autogenerated. Do not add tests below this line:
; CHECK: {{.*}}
