Haydn lit XFAIL owner / failure ledger
Durable map for live XFAIL directives under CodeGen/Haydn + MC/Haydn.
Re-query live files before citing counts.
Stamp: 2026-08-22 G004 dual-ON qualification (live=1: GE96-09 only).
  2026-08-22 G004: the four dual-ON combined XFAILs are REMOVED —
    hwloop-multistage-combined-{matrix,transaction,cfg,trip}.ll
  now PASS green. Dual-ON accepts and materializes with II parity
  (qualify parcels-per-iter == searched-II == AsmPrinter AchievedII;
  stage-mbb prolog/epilog peel MBBs; inclusive HWLR_END). Root causes
  fixed this wave: prolog-liveness observation point (preheader exit,
  not just-post-SET), F39 static-trip walk for every trip form
  (LoopStart op0 / reg-trip SET op3 / soft countdown), idle NOPs
  committed at empty kernel cycles (parcel-visibility parity),
  member-latency pin parity (two-copy edge upgrade + commit bake),
  stall-inclusive II certificate + residue dest-window law in
  placement, leaveMBB deferring to a multistage-committed MBB,
  PseudoLoopEnd excluded from the shared parcel counter, epilogue
  clone port-MRI context (parentless getMF crash), success-path
  policy remark. Product default for -haydn-enable-multistage-sms
  was OFF then; the SMS product default FLIPPED ON 2026-08-22 at
  cbd39b38437a (productDefaultEnabled() == true; qualified — this stamp
  paragraph is G004-era history).
  Prior stamps: 2026-08-18 T7-ALIGN residual hygiene + T8-ALIGN-POLICY
  (R15 leftover + P19 closed; W54/W55 LANDED; rebound ancestor
  98890c529be9). GE96-03 tests remain product PASS
  (cb89/cb94/reloc-range-branch).
  Mechanical parity: llvm/utils/haydn/check_xfail_ledger.py (+ lit pin
  xfail-owner-ledger-selfcheck.ll). Update this ledger in the same change as
  any add/remove of a live XFAIL directive.

TOTAL: 1

PATH: llvm/test/MC/Haydn/ar-unaligned-roundtrip.s
  OWNER:   GE96-09 (ar_sel=2/3 classification)
  FAILURE: ar_sel 2/3 product class unspecified; encode residual
  STATUS:  unsupported / alignment-open
  LIVE_GOAL: G-OBJECT-ABI / G-TEST-EVIDENCE
