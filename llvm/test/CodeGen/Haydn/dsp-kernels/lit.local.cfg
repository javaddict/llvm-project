# Captured DSP-kernel reference assemblies live here as .s snapshots (no RUN
# line). The real kernel tests are the .ll files. Exclude .s from discovery so
# the snapshots don't show up as Unresolved.
config.suffixes = ['.ll', '.c', '.cpp', '.mir']
